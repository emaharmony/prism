# Prism Repository Quality Report

```text
Repository:             github.com/emaharmony/prism
Commit:                 9a21215d114dd65caa539e9b381539708a6dbaa5 (main, at branch point)
Branch:                 release/v0.2.0-preview.1-docs
Candidate version:      v0.2.0-preview.1 (not tagged)
Last verified:          2026-07-15 15:18:57 (local, includes uncommitted protected-branch + shell-tool fixes on this branch) / see CI runs for authoritative Linux results
Verification environment: Windows 11, Go 1.26.3, Python 3.14.4 (system) / 3.12.10 (project venvs)
Git status:             Dirty (expected — active release-cleanup branch; clean at branch creation)
```

## Metrics
- **Go packages:** 85
- **Go test files:** 202
- **Go tests:** 1669
- **Go benchmarks:** 4
- **Integration tests:** 17
- **Python tests (Remembrance + SDK):** 13 (6 Remembrance, 7 SDK)
- **Build status:** Passing (`go build ./...`)
- **Lint status (vet):** Passing (`go vet ./...`)
- **Static analysis (staticcheck):** Passing, 0 findings
- **Vulnerability scan (govulncheck):** 3 reachable vulnerabilities, all in the Go standard library (`crypto/tls`, `net/textproto`, `crypto/x509`), fixed upstream in Go 1.26.4/1.26.5. No reachable vulnerabilities in Prism code or direct dependencies. Action: bump the Go toolchain used by CI and local development past 1.26.3.
- **Ruff (Python lint):** Passing, 0 findings (`remembrance/`, `sdk/`)
- **Documentation links:** Passing, 0 broken local links (`scripts/check-links.sh`) — 163 broken links found and fixed during this release-cleanup pass (see CHANGELOG)
- **Coverage gate (`scripts/coverage-gate.sh`):** Passing — **55.4% aggregate** (required 55.0%); critical packages: `internal/safety` 95.4%, `internal/policy` 92.5%, `internal/approval` 85.6%, `internal/mutation` 82.7%, `internal/validation` 90.6%, `internal/guard` 96.1%, `internal/tool` authority files 82.0% (all required ≥80.0%). Verified end-to-end on Windows after the shell-tool timeout-detection fix below.
- **Git status (tracked worktree):** Clean at last full verification pass on `main`

The metrics block above (through Git status) is regenerated by `scripts/quality-report.ps1`; run it before significant releases or after major architecture changes.

## Supported Platforms

| Platform | Level | Basis |
|---|---|---|
| Linux | Test-suite verified | CI `test-linux`: full `go test ./...` + coverage, plus race detector on 9 core packages |
| Windows | Basic / smoke tested | CI `test-windows`: build + `go test` on `internal/safety`, `internal/config`, `internal/run` only, plus `prism doctor` smoke test. The full suite is **not** run on Windows CI. |
| macOS | Build verified only | `scripts/build-release.sh` cross-compiles a `darwin/arm64` archive; there is no macOS CI job and no macOS test execution anywhere in this repository. |
| Python (Remembrance, SDK) | Test-suite verified | CI `test-python` runs `pytest` for Remembrance on Ubuntu; SDK tests (`sdk/tests/`) are not currently wired into CI (verified manually in an isolated venv for this report — see Known Limitations). |

Do not read "Windows (Basic)" as "Windows fully supported" — it means build-verified plus three packages' worth of test coverage plus a CLI smoke check, nothing more.

## Feature Stability Summary
- **Stable:** Workflow Engine, Event Bus, Policy Engine, Approval Gates, Validation Gates, Tool Registry, Providers.
- **Preview:** API, Dashboard, Scheduler, Discord Adapter, Worktree Mutation.
- **Experimental:** Remembrance, MCP, Cross-Prism, Sub-agents, Autopatch, Free Mode / Shell Tool (master-user-only, opt-in — see [Safety Model](./docs/concepts/SAFETY.md)).

See the full [Stability Matrix](./docs/reference/stability-matrix.md) for per-feature production recommendations.

## CI & Verification
- **Supported Platforms:** see table above.
- **Race Testing:** Enabled for `bus`, `event`, `run`, `workflow`, `safety`, `policy`, `agent`, `orchestrator`, `task` (see `.github/workflows/ci.yml`). Not enabled repo-wide.
- **Coverage gates:** `scripts/coverage-gate.sh` enforces a 55% aggregate floor and an 80% floor on `internal/{safety,policy,approval,mutation,validation,guard}` plus tool-authority files. This gate is **not** currently wired into any GitHub Actions workflow — it is a manual/pre-release check (`scripts/quality-report.sh --verify --race` or run directly). It now completes end-to-end on Windows (previously blocked by two Windows-only `internal/tool` test failures — `TestShellTool_Timeout` misdetected a killed-but-still-`*exec.ExitError`-reporting process as a normal exit rather than a timeout, and `TestShellTool_Cwd` used a POSIX-only `/tmp` path; both are fixed in `internal/tool/shell.go` and `shell_test.go`). Result: 55.4% aggregate, all critical packages ≥80% — see Metrics above. **Recommend also wiring this gate into Linux CI** so the aggregate/critical numbers are continuously verified rather than only on manual/local runs.
- **Static analysis:** `go vet` and `staticcheck` both pass with 0 findings on this commit.
- **Release archives:** `scripts/build-release.sh` was reproduced manually for this report (Linux amd64, Darwin arm64, Windows amd64 cross-compiles all succeed; SHA256 checksums verify). The script's own Windows-archive step depends on `zip`, which was not present in this local shell — this is a local-environment gap, not a script defect; the actual release workflow builds and archives via GitHub Actions on Ubuntu, where `zip`/`tar` are both available.
- **CI action pinning:** all three GitHub Actions workflows (`ci.yml`, `release.yml`, `external-links.yml`) pin third-party actions to immutable commit SHAs, verified against the upstream tags via the GitHub API as of this report.

## Known Limitations
- Prism is in **public preview**: source-available, local-first, and **not production-ready**. See [Stability Matrix](./docs/reference/stability-matrix.md) and [Public Preview Checklist](./docs/operations/PUBLIC_PREVIEW_CHECKLIST.md).
- Free Mode (owner-authorized mutation mode) grants a single configured Discord user account bypass of the approval-gate workflow for file, git, and shell mutations. File and directory writes still pass through independent workspace/write-root path containment at the tool layer even when auto-approved. The shell tool's working directory is **not** path-contained — only a small hard blocklist and the configured command-pattern tier restrict it. See [Safety Model](./docs/concepts/SAFETY.md) for the full breakdown.
- The `project.default_branch` config field is enforced by `GitCommitTool`/`GitPushTool` (`internal/tool/git.go`): those tools refuse to commit/push while the repo's current branch (or, for push, an explicitly targeted branch) equals the configured protected branch (default `"main"`), unconditionally — including under Free Mode. This is a single global value resolved from the default project (`Config.ProtectedBranch()`), not per-project; in multi-project deployments with differing `default_branch` values, only the default project's branch name is protected across every repo any git tool touches. See [Safety Model](./docs/concepts/SAFETY.md#free-mode-owner-authorized-mutation-mode).
- SDK Python tests (`sdk/tests/`) are not wired into any CI workflow; they were verified manually for this report in an isolated venv.
- External integrations (OpenAI, Anthropic, Gemini, Ollama, Discord, Codex CLI, Claude CLI, MCP servers, Firecrawl) are only as reliable as the third-party service and are not covered by Prism's own test suite beyond mocked/contract-level tests.

---
*The Metrics section (Go/Python counts, build/lint/git status) is auto-generated by `scripts/quality-report.ps1`. Everything else in this file is maintained by hand and should be reviewed at each release.*
