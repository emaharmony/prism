version: '3.8'

services:
  prizm:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./runs:/app/runs
      - ./policies:/app/policies
    environment:
      - PRIZM_PORT=8080
      - PRIZM_RUN_DIR=/app/runs
      - PRIZM_POLICY_DIR=/app/policies
    # For embedded NATS (no external NATS needed):
    # command: ["dashboard", "--embedded-bus"]
    command: ["dashboard"]