# v0.2.0-preview.1 Release Checklist

Candidate version prepared on `VERSION`, `internal/version/version.go`,
`CHANGELOG.md`, `sdk/pyproject.toml`, `remembrance/pyproject.toml`,
`sdk/prism/__init__.py`, and `remembrance/src/remembrance/__init__.py`.
**Not tagged.** Verified against commit `9a21215d114dd65caa539e9b381539708a6dbaa5`
on branch `release/v0.2.0-preview.1-docs` (created from `origin/main`).

Only check an item once it has actually been verified — see the note next to
each unchecked item for what's outstanding.

## Repository

- [x] Release branch created from a clean, up-to-date `origin/main`
- [x] No uncommitted changes at branch creation (worktree was clean; changes
      since are this release-cleanup pass itself)
- [x] No secrets in tracked files (no `.env`, keys, tokens found; `.gitignore`
      covers the standard patterns)
- [x] No stale tracked binaries (`git ls-files` contains no `.exe` or root
      `prism`/`prism-cli`/`prism-bus`/`prism-agent` binaries)
- [x] No broken internal documentation links (`scripts/check-links.sh` exits 0
      — 163 broken links found and fixed during this pass)
- [ ] Current dashboard screenshots — **not done**. No screenshots exist yet
      under `docs/assets/`; README links to a placeholder path. Take and add
      screenshots before announcing.
- [x] README reviewed and restructured (landing order, lifecycle diagram,
      safety model, stability evidence, binary-warning removed)
- [x] License reviewed (unchanged — all-rights-reserved, source-available;
      no license text modified)

## Quality

- [x] `go build ./...` passes
- [x] `go test ./... -count=1` — 1669 tests across 85 packages, **0
      failures** on Windows. (Previously 2 known Windows-only failures,
      `TestShellTool_Timeout`/`TestShellTool_Cwd`; fixed by correcting the
      shell tool's timeout-vs-error detection in `internal/tool/shell.go`
      and making the tests platform-portable.)
- [x] `go test -race` on the CI package scope (`bus`, `event`, `run`,
      `workflow`, `safety`, `policy`, `agent`, `orchestrator`, `task`) passes
- [x] Coverage gates (`scripts/coverage-gate.sh`, 55% aggregate / 80%
      critical-package floor) — **passes end-to-end on Windows**: 55.4%
      aggregate, all critical packages ≥80% (`internal/tool` authority files
      82.0%). Previously blocked by the same `internal/tool` test failures;
      now resolved. See [QUALITY.md](../../QUALITY.md) for the full
      breakdown. Still not wired into CI — recommend adding it there for
      continuous enforcement.
- [x] Python tests — 13 passing (6 Remembrance via `pytest`, 7 SDK via an
      isolated venv; SDK tests are not currently wired into CI)
- [x] Staticcheck — 0 findings
- [x] govulncheck — 3 reachable vulnerabilities, all Go-standard-library,
      fixed in Go 1.26.4/1.26.5 (current toolchain here is 1.26.3); no
      reachable vulnerabilities in Prism code or direct dependencies
- [x] Ruff — 0 findings (`remembrance/`, `sdk/`)
- [ ] actionlint — **not run**; not installed in this environment and not
      installed for this pass. Workflow YAML was validated for syntax only
      (`yaml.safe_load`).
- [x] Documentation links — `scripts/check-links.sh` passes (0 broken)
- [x] Golden-path workflow — `workflow run demo.echo_tool` verified manually
      via `prism doctor` and the existing echo-workflow test coverage
- [ ] Dashboard smoke test — **not run** in this pass (no browser
      verification performed; API/build verified only)

## Release

- [x] Version output correct (`prism version` → `prism v0.2.0-preview.1`,
      matches `VERSION` and the full `scripts/check-version.sh` contract)
- [x] CHANGELOG updated with a `[0.2.0-preview.1]` section distinguishing it
      from released `[0.1.0]` and empty `[Unreleased]`
- [x] Release notes drafted (`docs/releases/v0.2.0-preview.1.md`)
- [x] Release archives buildable — reproduced `scripts/build-release.sh`'s
      logic manually: Linux amd64, Darwin arm64, and Windows amd64 all
      cross-compile cleanly and SHA-256 checksums verify. The script itself
      wasn't run end-to-end locally (its Windows step needs `zip`, not
      present in this shell) — the actual release path runs via
      `release.yml` on Ubuntu, where both `zip` and `tar` are available.
- [ ] Checksums — verified manually for the reproduction above; not
      generated by the actual `scripts/build-release.sh` run (see above)
- [ ] Linux artifact — build-verified only, not run/smoke-tested
- [ ] Windows artifact — build-verified only, not run/smoke-tested
- [ ] macOS artifact — build-verified only (cross-compiled); no macOS CI job
      or macOS test execution exists anywhere in this repository
- [ ] Clean-clone verification — **not run** (would require cloning a fresh
      checkout; not done in this pass)
- [ ] Candidate tag — **not created**, per instructions
- [ ] GitHub Release draft — **not created**, per instructions

## Launch

- [ ] Dashboard screenshot — not captured (see Repository section)
- [ ] Architecture graphic — the README's execution-lifecycle diagram is
      text-based; no separate graphic exists
- [ ] Demo video — not created
- [ ] LinkedIn post — source material drafted at
      [docs/releases/LINKEDIN_LAUNCH_NOTES.md](./LINKEDIN_LAUNCH_NOTES.md);
      the actual post is not written
- [ ] Repository description / GitHub topics — not verified in this pass
      (requires GitHub repo settings access)
- [ ] Pinned repository — not verified
- [ ] Feedback issue or discussion channel — not verified/created

## Summary

**Ready after the remaining P2 items** (or acceptable to tag now if the P2s
are deferred to post-launch polish). All prior P0/P1 blockers are resolved:

- ~~**P0:** `project.default_branch` was documented as a protected branch
  but not enforced.~~ **Fixed** — `GitCommitTool`/`GitPushTool` now refuse
  to commit/push to it directly, unconditionally (including under Free
  Mode). See
  [Safety Model](../concepts/SAFETY.md#free-mode-owner-authorized-mutation-mode).
- ~~**P1:** `scripts/coverage-gate.sh` could not complete on Windows.~~
  **Fixed** — now passes end-to-end with real numbers (55.4% aggregate, all
  critical packages ≥80%). See [QUALITY.md](../../QUALITY.md).
- ~~**P1:** `TestShellTool_Timeout` / `TestShellTool_Cwd` failed on
  Windows.~~ **Fixed** — root cause was `shell.go` inferring timeout from
  exit code alone instead of checking the context deadline; both tests now
  pass and are portable across platforms.
- **P2 (still open):** No dashboard screenshots, demo video, or clean-clone
  verification yet — needed before the LinkedIn announcement, not before
  tagging.
- **P2 (still open):** Coverage gate is not wired into CI (manual/local
  check only) — recommend adding it to a workflow for continuous
  enforcement.
