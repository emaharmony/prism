# Design system

`app.css` defines the dashboard’s dark-first semantic tokens: surfaces, text,
borders, accent, success, warning, danger, and info states; a consistent spacing
scale; responsive breakpoints; focus treatment; status badges; tables; cards; and
loading, empty, and error states. Status is always communicated with text and an
icon as well as color.
