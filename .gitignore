# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Prism build artifacts (binaries at repo root only)
*.exe
/prism
/prism-cli
/prism-bus
/prism-agent
prism-data/

# Environment & secrets
.env
.env.*
*.pem
*.key
*.p12
*.jks
*.keystore
credentials.json
secrets.json
tokens.json

# OS & temp files
.DS_Store
Thumbs.db
*.tmp
*.bak
*.log

# Python build/test artifacts
__pycache__/
*.egg-info/
.pytest_cache/
.venv/

# Database files
*.db
*.db-wal
*.db-shm

# Run outputs (keep directory structure)
runs/*
!runs/.gitkeep
# Sanitized example artifacts are intentionally tracked
!examples/runs/
!examples/runs/**

# Scratch / temp helpers
tmp_natspub/
tmp/
.tmp/
dist/
build/
coverage/
coverage.out
coverage-gate.md
test-results/
/prism-panel
.prism/

# Local config (contains secrets)
prism.yaml

# Workspace — local state, never shipped
prism-workspace/
docs/PUDGYPOWER-FACTORY-RUNBOOK.md
/campaign
/references

# .junie and other session artifacts
/.junie/
/.claude/
/.codex/
/.gemini/
/.output.txt
/~

# AI assistant tooling — local state, chat history, and caches (not project source).
.cursor/
.aider*
.specstory/
.continue/
.windsurf/
.cline/
.roo/
.serena/
.codeium/
# Remove the next line if your team commits a shared MCP config:
.mcp.json
